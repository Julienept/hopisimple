{% extends 'base.html.twig' %}

{% block title %}S'inscrire{% endblock %}

{% block body %}

<div class="container">
<h1>Inscrivez-vous</h1>

{{ form_start(userForm)}}
{{ form_widget(userForm)}}

<button type="submit" class="btn btn-primary">Valider</button>
{{ form_end(userForm)}}</div>

{% endblock body %}