{% extends 'base.html.twig' %}

 {% block title %}Votre message n°{{ contact.id }}{% endblock %}

 {% block body %}

<div class="container">

    <h1>Votre message a bien été envoyé !</h1>

  <div>
  <h6>Votre message :</h6>
  <p> {{ contact.message }}
  </p>  
  <p>Le {{ contact.createdAt|date('d/m/Y H:i') }}</p>
  </div>

  <div>
  <h6>Vos coordonnées :</h6>
  <p>{{ contact.fullname }}</p>
  <p>{{ contact.email }}</p>
  </div>



</div>

  {% endblock %}
